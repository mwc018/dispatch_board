{
  "name": "dispatch-board",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install:server": "cd server && npm install --ignore-scripts",
    "start": "cd server && node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
